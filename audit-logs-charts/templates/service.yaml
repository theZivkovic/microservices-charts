apiVersion: v1
kind: Service
metadata:
  name: {{ include "audit-logs-charts.fullname" . }}
  labels:
    {{- include "audit-logs-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "audit-logs-charts.selectorLabels" . | nindent 4 }}
