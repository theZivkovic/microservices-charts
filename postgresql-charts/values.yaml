# Define authentication parameters
auth:
  existingSecret: "microservices-secrets-postgresql"
  secretKeys:
    userPasswordKey: "password"

primary:
  extraEnvVars:
    - name: POSTGRES_USER
      valueFrom:
        secretKeyRef:
          name: "microservices-secrets-postgresql"
          key: "username"
    - name: POSTGRES_DATABASE
      valueFrom:
        secretKeyRef:
          name: "microservices-secrets-postgresql"
          key: "database"
readReplicas:
  extraEnvVars:
    - name: POSTGRES_USER
      valueFrom:
        secretKeyRef:
          name: "microservices-secrets-postgresql"
          key: "username"
    - name: POSTGRES_DATABASE
      valueFrom:
        secretKeyRef:
          name: "microservices-secrets-postgresql"
          key: "database"

persistence:
  enabled: true
  storageClass: "standard"
  size: "10Gi"

# Configure service exposure
service:
  type: ClusterIP
  port: 5432

volumePermissions:
  enabled: true

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

extraEnvVars:
  - name: PG_TIMEZONE
    value: "UTC"
